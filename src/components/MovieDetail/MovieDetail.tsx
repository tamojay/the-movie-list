import React from "react";
import styles from "./MovieDetail.module.css";

const MovieDetail: React.FC = () => {
  return (
    <div className={styles.movieDetail}>{/* Genre filter UI elements */}</div>
  );
};

export default MovieDetail;
